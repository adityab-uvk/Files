SELECT
    CartItemId,
    UserCode,
    CompanyCode,
    Email,
    Material_Code,
    Material_Description,
    Quantity,
    CartId,
    Rate,
    Qty_Into_Rate_Amt,
    PackSize,
    Division
FROM TB_Cart_Items
WHERE CompanyCode = 1070
ORDER BY CartItemId DESC;
