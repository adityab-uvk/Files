SELECT 
    Condition_Table,
    Condition_Type,
    COUNT(*) AS RecordCount
FROM TB_PriceMaster
WHERE CompanyCode = 1070
  AND SalesOrganisation = 'MCPL'
  AND Condition_Type IN ('ZBAG', 'ZDIS')
GROUP BY 
    Condition_Table,
    Condition_Type
ORDER BY 
    Condition_Table,
    Condition_Type;
